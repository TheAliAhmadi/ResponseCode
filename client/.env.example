# Default for same-origin Worker API routing on Cloudflare Pages.
VITE_API_BASE=/api

# Optional direct API URL override for local debugging without Vite proxy.
# VITE_API_BASE=http://127.0.0.1:8787/api
